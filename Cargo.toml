[workspace]
members = ["trench-codec", "trench-core", "trench-live", "xtask"]
resolver = "2"

# Dependencies compile at full optimization; your crate stays debug-fast
[profile.dev.package."*"]
opt-level = 3
