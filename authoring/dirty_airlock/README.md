# DIRTY Airlock

This zone is the canonical pointer to DIRTY provenance.

## Primary Upstream Source

- `C:\Users\hooki\trench_re_vault`

## Hard Rule

DIRTY material is never shipping runtime truth and never a clean authoring surface.

Allowed across the boundary:

- sanitized behavioral contracts
- sanitized handoffs
- test vectors
- acceptance thresholds

Forbidden across the boundary:

- raw binaries
- ROM dumps
- disassembly/decompile structure
- symbol tables
- raw lifted tables presented as clean truth

## Why This Zone Exists

The hub must make DIRTY provenance reachable without lying about what it is.
This folder is that explicit boundary marker.

